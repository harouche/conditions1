#include <stdio.h>
#include <stdlib.h>

int main()
{
    int N;
      printf("veuillez saisir un nomber:");
        scanf ("%d",&N);
    if (N%2==0)
        printf("est un nomder pair");
    else
        printf("est un nomber impair");
      return 0;
}
