#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
    int a,b,c;

       printf("donner moi le 1er entier");
       scanf("%d",&a);

          printf("donner moi le 2er entier");
       scanf("%d",&b);

        printf("donner moi le 3er entier");
       scanf("%d",&c);

    float delta=pow(b,2)-4*a*c;
         printf("donner moi la 1er");

        if(delta<0);
         printf("n'a pas de solution");

        if(delta>0); }

            float x1,x2;
     x1=   (-b-sqrt(delta))/ (2*a);
     x2=   (-b+sqrt(delta))/ (2*a);
          printf("deux solution");
        printf("deux solution");

        }
         if(delta=0)
            printf("il a une seul solution: %.2f",-/2*a);
}
